{{/*
  ----------------------------------------------------------------------
  robots.txt — explicit, crawl-friendly defaults
  ----------------------------------------------------------------------
  Hugo emits this file at the site root when `enableRobotsTXT: true`
  (set in hugo.yaml). The default Hugo template is a single
  "User-agent: *" + "Allow: /" line; this template is fuller:

    - Names the sitemap explicitly (Google, Bing, Yandex all use this).
    - Whitelists everything by default — this is a static docs site
      with no admin / staging surface to hide.
    - Disallows the few paths that exist purely as build artefacts.
    - Adds a `Host:` directive (Yandex still respects this).
    - Records when the file was generated so a stale CDN cache is
      visible to operators.

  To exclude a section from indexing, set `noindex: true` on the
  section's `_index.md` frontmatter — the SEO partial will emit
  `<meta name="robots" content="noindex,nofollow">` and search
  engines will drop it from the index even without a robots.txt
  rule. robots.txt blocks crawl; the meta blocks indexing — the
  difference matters when a page is linked from elsewhere.
*/}}
# robots.txt for {{ site.Title }}
# Generated by Hugo at build time.

User-agent: *
Allow: /
Disallow: /search/
Disallow: /tags/null/
Disallow: /tags/Null/

# AI / LLM crawlers — explicitly allowed.
# This site is a public recipe library *intended* to be reached by
# agents. Allowing these crawlers improves discoverability inside
# AI assistants (Google AI Overviews, Perplexity citations, ChatGPT
# browse, Claude's citation surface). `Disallow:` with an empty
# value is robots.txt for "allow everything for this user-agent."
#
# To block any of these instead, change `Disallow:` to `Disallow: /`
# on the relevant line.
User-agent: GPTBot
Disallow:

User-agent: ClaudeBot
Disallow:

User-agent: Claude-Web
Disallow:

User-agent: Google-Extended
Disallow:

User-agent: PerplexityBot
Disallow:

User-agent: anthropic-ai
Disallow:

User-agent: cohere-ai
Disallow:

User-agent: CCBot
Disallow:

# Sitemap (Hugo auto-generates this at the site root).
Sitemap: {{ "sitemap.xml" | absURL }}

# Host hint (Yandex). Modern Google ignores this; harmless to keep.
Host: {{ replace site.BaseURL "https://" "" | replace "http://" "" | strings.TrimRight "/" }}
